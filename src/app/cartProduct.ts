export class cartProducts {
    constructor(public size:string, public quantity:number, public email: string,public name:string, public price:number, public type: string, public image:string,public id:number) {}
}